version: "3.8"

services:
  nginx:
    container_name: nginx
    build: ./requirements/nginx
    image: nginx
    volumes:
      - wp_data:/var/www/html/wordpress
      - portfolio_data:/var/www/html/portfolio
      - ./requirements/nginx/conf/nginx.conf:/etc/nginx/http.d/nginx.conf
    ports:
      - "443:443"
    restart: always
    depends_on:
      - wordpress
    networks:
      - InceptionNetwork
  mariadb:
    container_name: mariadb
    build: ./requirements/mariadb
    image: mariadb
    environment:
      WP_DB_NAME : ${WP_DB_NAME}
      DB_USER : ${DB_USER}
      DB_PASSWD : ${DB_PASSWD}
      DB_ROOT_PASSWD : ${DB_ROOT_PASSWD}
    ports:
      - "6969:3306"
    volumes:
      - db_data:/var/lib/mysql
      - ./requirements/mariadb/conf/config.sh:/config.sh
    restart: always
    networks:
      - InceptionNetwork
  wordpress:
    container_name: wordpress
    build: ./requirements/wordpress
    image: wordpress
    environment:
      WP_HOST: mariadb:3306
      WP_TITLE: ${WP_TITLE}
      WP_URL: ${WP_URL}
      WP_ADMIN: ${WP_ADMIN}
      WP_ADMIN_PASSWD: ${WP_ADMIN_PASSWD}
      WP_ADMIN_EMAIL: ${WP_ADMIN_EMAIL}
      WP_DB_NAME: ${WP_DB_NAME}
      WP_USER: ${WP_USER}
      WP_PASSWD: ${WP_PASSWD}
    depends_on:
      - mariadb
    ports:
      - "9000:9000"
    volumes:
      - wp_data:/var/www/html/wordpress
      - ./requirements/wordpress/tools/setup_wp.sh:/setup_wp.sh
    restart: always
    networks:
      - InceptionNetwork
  portainer:
    container_name: portainer
    build: ./requirements/bonus/portainer/
    image: portainer
    restart: always
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    networks:
      - InceptionNetwork
  portfolio:
    container_name: portfolio
    build: ./requirements/bonus/portfolio/
    image: portfolio
    restart: always
    volumes:
      - portfolio_data:/var/www/html/portfolio
    networks:
      - InceptionNetwork

networks:
  InceptionNetwork:
    driver: bridge

volumes:
  db_data:
    driver_opts:
      o: bind
      type: none
      device: /home/yelaissa/data/db
      # device: /Users/yusufisawi/data/db
  wp_data:
    driver_opts:
      o: bind
      type: none
      device: /home/yelaissa/data/wp
      # device: /Users/yusufisawi/data/wp
  portainer_data:
    driver_opts:
      o: bind
      type: none
      device: /home/yelaissa/data/portainer
      # device: /Users/yusufisawi/data/portainer
  portfolio_data:
    driver_opts:
      o: bind
      type: none
      device: /home/yelaissa/data/portfolio
      # device: /Users/yusufisawi/data/portfolio